"use client";

const ProjectsHoldings = () => {
   return (
      <section>
         <div className="header flex flex-col">
            <p className="text-text">What each project is actually paying you</p>
            <p className="text-text-muted">
               Top bar is the fee and where it has got to. Thin bar underneath
               is hours against what you quoted.
            </p>
         </div>
      </section>
   );
};

export default ProjectsHoldings;
